/**
 * 1. Crea una función que calcule el área de un rectángulo a partir de su base y altura, y la muestre en consola.
 */
double area(double base, double altura) => base * altura;

void main() {
  double base = 2;
  double altura = 4.3;
  print(area(base, altura));
}